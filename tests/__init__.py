"""Tests for the portable recon toolkit.

Run with:

    python -m unittest discover -s tests -v

All tests use stdlib only and never reach the network; resolvers and
sockets are monkey-patched with canned responses.
"""
